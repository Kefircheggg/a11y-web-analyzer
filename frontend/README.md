# Accessibility Analyzer - Frontend (Chrome Extension)

Chrome расширение для анализа доступности веб-страниц с помощью axe-core и AI-рекомендаций.

## 🎯 Возможности

- 🔍 Автоматический анализ доступности с помощью axe-core
- 🤖 AI-рекомендации по улучшению доступности от backend
- 👁️ Визуальная подсветка проблемных элементов на странице
- 📊 Детальные отчеты с классификацией по уровням критичности
- 💡 Подсказки с описанием проблем и способами их решения

## 🚀 Установка

### Автоматическая установка

```bash
chmod +x install.sh
./install.sh
```

### Ручная установка

```bash
# Создать директории
mkdir -p icons libs/axe-core

# Скачать axe-core
curl -L https://cdnjs.cloudflare.com/ajax/libs/axe-core/4.8.2/axe.min.js -o libs/axe-core/axe.min.js

# Добавить иконки в папку icons/ (icon16.png, icon48.png, icon128.png)
```

## 🔧 Настройка

### 1. Настроить URL backend

Откройте `background/background.js` и укажите URL вашего backend:

```javascript
const API_URL = 'http://localhost:3001/api/v1/analyze';
```

### 2. Загрузить расширение в Chrome

1. Откройте `chrome://extensions/`
2. Включите **"Режим разработчика"** (Developer mode)
3. Нажмите **"Загрузить распакованное расширение"**
4. Выберите эту папку (`frontend`)

## 📖 Использование

1. Откройте веб-страницу для анализа
2. Нажмите на иконку расширения в панели Chrome
3. Нажмите **"Analyze Page"**
4. Дождитесь результатов (откроется новая вкладка)
5. Просмотрите отчет с проблемами доступности
6. (Опционально) Нажмите **"Show Highlights"** для подсветки элементов

## 📁 Структура

```
frontend/
├── manifest.json          # Конфигурация расширения
├── install.sh             # Скрипт установки
├── background/
│   └── background.js      # Background service worker
├── popup/
│   ├── popup.html         # UI popup окна
│   ├── popup.css          # Стили popup
│   └── popup.js           # Логика popup
├── content/
│   ├── content.js         # Скрипт для подсветки элементов
│   └── highlight.css      # Стили подсветки
├── results/
│   ├── results.html       # Страница результатов
│   ├── results.css        # Стили результатов
│   └── results.js         # Логика отображения
├── utils/
│   └── storage.js         # Утилиты для Chrome Storage
├── icons/                 # Иконки расширения
│   ├── icon16.png
│   ├── icon48.png
│   └── icon128.png
└── libs/
    └── axe-core/
        └── axe.min.js     # Библиотека axe-core
```

## 🎨 Цветовая схема подсветки

- 🔴 **Critical** - красный (#FF0000)
- 🟠 **Serious** - оранжевый (#FF6600)
- 🟡 **Moderate** - жёлтый (#FFAA00)
- 🔵 **Minor** - синий (#0066FF)

## 🔌 Backend API

Расширение взаимодействует с backend через REST API:

**Endpoint:** `POST /api/v1/analyze`

**Request:**
```json
{
  "url": "https://example.com",
  "violations": [...]
}
```

**Response:**
```json
{
  "id": "job-uuid",
  "url": "https://example.com",
  "status": "processing",
  "progress": 0
}
```

Подробнее см. [../backend/README.md](../backend/README.md)

## 🐛 Troubleshooting

### Расширение не работает
- Проверьте консоль: F12 → Console
- Убедитесь, что `libs/axe-core/axe.min.js` загружен
- Проверьте права в `manifest.json`

### Подсветка не работает
- Убедитесь, что content script загружен
- Проверьте, что вы нажали "Show Highlights"
- Посмотрите ошибки в консоли страницы

### Ошибка связи с backend
- Проверьте, что backend запущен (`http://localhost:3001/health`)
- Убедитесь в правильности URL в `background/background.js`
- Проверьте CORS на backend

## 📝 Разработка

### Перезагрузка после изменений

1. Внесите изменения в код
2. Откройте `chrome://extensions/`
3. Нажмите кнопку обновления (🔄) под расширением

### Отладка

- **Popup**: ПКМ на иконку → "Проверить всплывающее окно"
- **Background**: `chrome://extensions/` → "Service worker" → "проверить"
- **Content script**: F12 на странице → Console
- **Results page**: F12 на странице результатов

## 📄 Лицензия

MIT
